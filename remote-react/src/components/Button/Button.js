import * as React from "react";
import * as ButtonStyles from "./Button.styles";

const Button = () => {
  return <ButtonStyles.Button>button from remote-react</ButtonStyles.Button>;
};

export default Button;
