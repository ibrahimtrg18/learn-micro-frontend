import * as React from "react";
import * as ButtonStyles from "./Button.styles";

const Button = () => {
  return <ButtonStyles.Button>button from remote_react</ButtonStyles.Button>;
};

export default Button;
